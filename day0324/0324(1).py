#calculate equation by using numericall method
#mx''+cx'+kx = Focos(wt)--->exponential, sin, cos
#damping harmonic osillator with external force
#LRC circuit ---> in ideal case == harmonic Osillator
#dQ/dt = I 전자의 흐름이 전류

#(integral)E dl = -d(magnetic flux)/dt
#(integral)E dl = (integral)[B,A]E dl{through the wire} + (integral)[A,B]E dl{through the air}
# V = L*di/dt (페러데이 법칙)

# in Lc circuit has no external force
# Vc+Vind = 0
# Vind = L*di/dt, Vcap = q/C
# L*di/dt + q/C = 0
# L*d^2q/dt^2 + q/C = 0
# L*Q'' + q/C = 0

# in LRC circuit resistance acting like damper
#   